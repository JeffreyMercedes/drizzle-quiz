# Void Server Deployment Environment Variables
# Copy this file to .env on void before deploying

# Database password - REQUIRED: Generate with: openssl rand -base64 32
DB_PASSWORD="your-secure-database-password-here"

# NextAuth Configuration
# Using Tailscale serve HTTPS on port 8443
NEXTAUTH_URL="https://void.tail517c12.ts.net:8443"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
