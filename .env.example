# Database
# For development (local PostgreSQL or docker-compose.dev.yml):
DATABASE_URL="postgresql://drizzle:drizzle_dev_password@localhost:5432/drizzle?schema=public"

# For production (docker-compose.yml uses this internally):
DB_PASSWORD="your-secure-password-here"

# NextAuth
# Development:
NEXTAUTH_URL="http://localhost:3000"

# Production: Set to your actual domain
# NEXTAUTH_URL="https://your-domain.com"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
